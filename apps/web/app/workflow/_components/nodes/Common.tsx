import { TaskParamType } from "@/types/task";

export const ColorForHandle: Record<TaskParamType,string> = {

    BROWSER_INSTANCE: "!bg-sky-500",
    STRING: "!bg-teal-500",
    SELECT: "!bg-amber-500",
    CREDENTIAL: "!bg-rose-500",
}